repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
        args: [--allow-multiple-documents]
    -   id: check-merge-conflict
-   repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.15.7
    hooks:
      # Run the linter.
    -   id: ruff-check
        types_or: [python, pyi{% if include_notebooks %}, jupyter{% endif %}]
        # Run the formatter.
    -   id: ruff-format
        types_or: [python, pyi{% if include_notebooks %}, jupyter{% endif %}]{% if use_rumdl %}
-   repo: https://github.com/rvben/rumdl-pre-commit
    rev: v0.1.58
    hooks:
      - id: rumdl      # Lint only (fails on issues)
      - id: rumdl-fmt  # Auto-format and fail if issues remain{% endif %}
